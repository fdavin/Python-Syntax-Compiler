def fungsi(var):
    x+=1
    return 3

class classify:
    def makeclass(classname):
        classmade = "K01"
        return classmade
b = "tes123"
a = fungsi(b)

with open('folderku', 'r') as file:
    x = file.read("tes123")